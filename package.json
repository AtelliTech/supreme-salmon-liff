{
  "name": "supreme-salmon",
  "version": "0.1.0-alpha.4",
  "scripts": {
    "dev": "vite --port 9000",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist --listen 3000",
    "start:https": "serve dist --listen 9000 --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "release": "release-it",
    "test": "vitest run api/index.test.js"
  },
  "dependencies": {
    "@line/liff": "2.27.3",
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "release-it": "^19.2.4",
    "serve": "^14.2.6"
  },
  "devDependencies": {
    "vite": "7.3.1",
    "vitest": "^4.1.2"
  }
}
